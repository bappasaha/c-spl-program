/* C Program to find factorial of a given number using recursion*/
#include <stdio.h>

int main()
{
    int a;
    for(a=1;a<=255;a++)
    {
        printf("%d = %c",,%c);
    }

    return 0;
}
